# CLAUDE.md

作業前に必ず [AGENTS.md](./AGENTS.md) を読んでください。
このリポジトリのルール・役割分担・MCPツール一覧はすべてそちらに集約しています。

Claude Code固有の補足のみ以下に書きます。

## サブエージェント

| エージェント | 用途 |
|---|---|
| `common-manual` | 共通マニュアルに基づく素材確認・提出前チェック・ルール照会 |
| `project-manual` | 案件独自ルールを共通マニュアルへ上書きして判断 |

## MCPサーバー

`.mcp.json` により `video-manual` サーバーが自動接続されます。
ルールを調べるときは記憶で答えず、必ず `manual_search` などのツールを使ってください。

## この環境でできないこと

- 画像・図解の生成（Codex側の担当）
- Premiere Proの操作（ローカルのUXPプラグイン側の担当）
